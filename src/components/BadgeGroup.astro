---
import Badge from "./Badge.astro";

type Props = {
  categories: string[];
};

const { categories } = Astro.props;
---

<div class="flex gap-2 flex-wrap">
  {categories.map((category) => <Badge>{category}</Badge>)}
</div>
